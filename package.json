{
  "name": "api-backend",
  "version": "1.0.0",
  "description": "API REST para el proyecto de turismo usando json-server.",
  "main": "index.js",
  "scripts": {
    "start": "json-server --port ${PORT} --static \"\" datos.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json-server",
    "render"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}